[package]
name = "leopardwm-watchdog"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Process supervisor for the LeopardWM daemon — restarts on crash and reports fail-closed recovery limits"

[[bin]]
name = "leopardwm-watchdog"
path = "src/main.rs"

[dependencies]
leopardwm-ipc = { path = "../ipc" }
leopardwm-platform-win32 = { path = "../platform_win32" }
anyhow = { workspace = true }
tracing = { workspace = true }
tracing-appender = { workspace = true }
tracing-subscriber = { workspace = true }
windows = { workspace = true }

[lints]
workspace = true
