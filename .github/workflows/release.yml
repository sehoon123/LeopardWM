name: Release

on:
  push:
    tags: ["v*"]

permissions:
  contents: read

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build:
    # Every `v*` tag builds here, including personal `-sehoon.N` builds: the
    # separate pipeline they used to rely on was retired, which left those tags
    # producing no assets at all. Any tag carrying a pre-release suffix is
    # published as a pre-release (see the `channel` step below).
    permissions:
      contents: write
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: dtolnay/rust-toolchain@631a55b12751854ce901bb631d5902ceb48146f7 # stable
        with:
          components: rustfmt, clippy
      - name: Install cargo-wix
        run: cargo install cargo-wix --version ^0.3 --locked
      - name: Verify formatting
        run: cargo fmt --all -- --check
      - name: Run full test suite
        run: cargo test --workspace --all-targets --locked
      - name: Run Clippy with warnings denied
        run: cargo clippy --workspace --all-targets --locked -- -D warnings
      - name: Build optimized binaries
        run: cargo build --workspace --release --locked
      - name: Verify GUI executable subsystems
        shell: pwsh
        run: ./.github/verify-gui-subsystems.ps1 -RepoRoot (Get-Location).Path
      - name: Package ZIP
        shell: bash
        run: |
          VERSION=${GITHUB_REF_NAME#v}
          DIR="LeopardWM-${VERSION}-x86_64-windows"
          mkdir "$DIR"
          cp target/x86_64-pc-windows-msvc/release/leopardwm.exe "$DIR/"
          cp target/x86_64-pc-windows-msvc/release/leopardwm-cli.exe "$DIR/"
          cp target/x86_64-pc-windows-msvc/release/lwm.exe "$DIR/"
          cp target/x86_64-pc-windows-msvc/release/leopardwm-watchdog.exe "$DIR/"
          cp README.md LICENSE CHANGELOG.md "$DIR/"
          7z a "${DIR}.zip" "$DIR"
      - name: Build MSI installer
        shell: bash
        run: |
          VERSION=${GITHUB_REF_NAME#v}
          cargo wix -p leopardwm-daemon -I wix/main.wxs \
            --target x86_64-pc-windows-msvc \
            --no-build --nocapture \
            --bin-path "C:\Program Files (x86)\WiX Toolset v3.14\bin"
          cp target/wix/*.msi "LeopardWM-${VERSION}-x86_64.msi"
          ls -l "LeopardWM-${VERSION}-x86_64.msi"
      - name: Classify release channel
        id: channel
        shell: bash
        run: |
          VERSION=${GITHUB_REF_NAME#v}
          # 0.2.7 -> release; 0.2.7-rc1 / 0.2.6-sehoon.17 -> pre-release.
          if [[ "$VERSION" == *-* ]]; then
            echo "prerelease=true" >> "$GITHUB_OUTPUT"
          else
            echo "prerelease=false" >> "$GITHUB_OUTPUT"
          fi
      - name: Extract changelog
        id: changelog
        shell: bash
        run: |
          VERSION=${GITHUB_REF_NAME#v}
          NOTES=$(awk "/^## \\[?${VERSION//./\\.}\\]?/{found=1; next} /^## /{if(found) exit} found{print}" CHANGELOG.md 2>/dev/null || true)
          if [ -z "$NOTES" ]; then
            echo "body=" >> "$GITHUB_OUTPUT"
          else
            EOF=$(dd if=/dev/urandom bs=15 count=1 status=none | base64)
            echo "body<<$EOF" >> "$GITHUB_OUTPUT"
            echo "$NOTES" >> "$GITHUB_OUTPUT"
            echo "$EOF" >> "$GITHUB_OUTPUT"
          fi
      - name: Generate SHA-256 checksums
        shell: bash
        run: |
          sha256sum LeopardWM-*.zip LeopardWM-*.msi > checksums.txt
          cat checksums.txt
      - uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228 # v3.0.2
        with:
          files: |
            LeopardWM-*.zip
            LeopardWM-*.msi
            checksums.txt
          body: ${{ steps.changelog.outputs.body }}
          generate_release_notes: ${{ steps.changelog.outputs.body == '' }}
          prerelease: ${{ steps.channel.outputs.prerelease == 'true' }}
